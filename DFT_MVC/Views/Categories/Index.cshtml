@using DFT_MVC.Data
@model IEnumerable<Category>

@{
	ViewData["Title"] = "Index";
	var alertList = TempData["ResultMessage"] as IEnumerable<string>;
}
<link rel="stylesheet" href="css/site.css">
<link rel="stylesheet" href="css/alertMsg.css">


<h1>Lista Kategorii</h1>

@if (alertList != null)
{
	var delete = "#D0342C"; // czerwony
	var create = "#53D02C"; // zielony
	var update = "#2C82D0"; // niebieski
	var color = string.Empty;

	foreach (var alert in alertList!)
	{
		if (alert.EndsWith("1"))
		{
			color = create;
		}
		else if (alert.EndsWith("2"))
		{
			color = update;
		}
		else if (alert.EndsWith("3"))
		{
			color = delete;
		}
	}

	<div class="alert">
		<style>
			.alert {
				background-color: @color; /* green */
			}
		</style>
		<span class="closebtn" onclick="this.parentElement.style.display='none';">&times;</span>
		@foreach (var item in alertList!)
		{
			<a>@item.Remove(item.Length - 1)</a>
		}
	</div>
}

<p class="container">
	<div class="align-left">
		<a class="btn btn-success" asp-action="Create">Create New</a>
	</div>
</p>
<table class="table">
	<thead>
		<tr>
			<th>Nazwa</th>
			<th>Zdjęcie</th>
			<th>Data utworzenia</th>
			<th></th>
		</tr>
	</thead>
	<tbody>
		@foreach (var item in Model)
		{
			<tr>
				<td>
					<a asp-controller="Subcategories" asp-action="Index" asp-route-id="@item.Id">
						@Html.DisplayFor(modelItem => item.Name)
					</a>
					
				</td>
				<td>
@*					@if (item.ImageData != null)
					{
						<img src="/Images/ThumbnailSmall/@item.ImageData.Id">
					}*@
					@if (item.ImageOriginal != null)
					{
						<img src="/Categories/ImageSmall/@item.Id">
						
					}
					else
					{
						<img src="~/images/no_image.png">
					}
				</td>
				<td>
					<a asp-controller="Categories" asp-action="Index1" asp-route-id="@item.Id">
					@Html.DisplayFor(modelItem => item.CreationDate)

					</a>
				</td>
				<td>
					<a asp-action="Edit" asp-route-id="@item.Id">Edit</a>
					<a asp-action="Details" asp-route-id="@item.Id">Details</a>
					<a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
				</td>
			</tr>
		}
	</tbody>
</table>
